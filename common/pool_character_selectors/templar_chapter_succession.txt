pool_templar_chapter_succession = {
	valid_character = {
		is_capable_adult = yes
		is_imprisoned = no
		is_married = no
		is_betrothed = no
		scope:base.faith = { has_allowed_gender_for_clergy = root }
		NOT = { has_trait = excommunicated }
		templar_holy_order_election_banned_trait_trigger = no
		is_courtier_of = scope:base
		OR = {
			has_character_flag = templar_senior_knight
			has_court_position = templar_senior_knight_court_position
		}
	}

	character_score = {
		base = 1
		modifier = {
			add = 10000
			scope:base ?= {
				exists = var:templar_chapter_nominee
				var:templar_chapter_nominee = root
			}
		}
		modifier = {
			add = 1000
			has_court_position = templar_senior_knight_court_position
		}
		compare_modifier = {
			value = martial
		}
		compare_modifier = {
			value = prowess
		}
		modifier = {
			add = 50
			scope:base.faith = { has_preferred_gender_for_clergy = root }
		}
	}

	config = {
		background = commander
		age = { 25 45 }
	}

	selection_count = 10
	pool = faith
	gender = clergy
	court = yes
}
